import InterfaceAccountPayable from "../interfaces/InterfaceAccountPayable"
import prisma from "../../../middlewares/connPrisma"

async function create(accountPayable : InterfaceAccountPayable) {
    try {
        
    } catch (error: any) {
        throw new Error(error)
    }
  
}